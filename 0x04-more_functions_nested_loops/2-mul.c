#include "main.h"

/**
 * mul- Multiply numbers
 * @a: Number to b evaluated
 * @b: Number to b evaluated
 *
 * Return: Always 1 on success
 */
int mul(int a, int b)
{
int answer;
answer = a * b;
return (answer);
}	
